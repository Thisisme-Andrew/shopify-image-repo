export const Image = ({
  id,
  title,
  userId,
  publicAccess,
  url,
  dateAdded
}) => {
 return ({
  id: id,
  title: title,
  userId: userId,
  publicAccess: publicAccess,
  url: url,
  dateAdded: dateAdded,
 })
}

export default Image;